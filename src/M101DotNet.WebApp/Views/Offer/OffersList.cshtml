@model WebApp.Models.Offer.OfferListViewModel

@{
    ViewBag.Title = "Offers list";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4 class="orange lighten-2 card-panel">@ViewBag.Title</h4>

@for (int i = Model.OffersList.Count-1; i >= 0 ; i--)
{    
        <div class="grey lighten-3 card-panel">
                <div class="offer-title">
                    <div class="inline-field">
                        <div class="card-panel orange lighten-3">
                            <span>@Model.OffersList[i].Offer.Name</span>
                        </div>
                    </div>
                    <div class="inline-field">
                        <div class="card-panel orange lighten-3">
                            <span>@Model.OffersList[i].Offer.Salary</span> PLN                            
                        </div>
                    </div>
                </div>
                <div class="offer-buttons">
                    @Html.ActionLink("Details", "Details", "Offer", new { id = Model.OffersList[i].Offer.Id }, new { @class = "btn-large waves-effect waves-light" })
                    @Html.ActionLink("Edit", "Edit", "Offer", new { id = Model.OffersList[i].Offer.Id }, new { @class = "btn-large waves-effect waves-light" })  
                    <a href="#remove-modal" class="btn-large waves-effect waves-light btn modal-trigger">Remove</a>
                </div>
                <div class="skill-list">                    
                        @foreach (var topSkill in Model.OffersList[i].TopSkills)
                        {
                            <div class="inline-field">
                                <div class="card-panel amber lighten-4">
                                    @topSkill.Name
                                    @topSkill.Level
                                </div>
                            </div>
                        }
                </div>
        </div>    
        @Html.Partial("_RemoveModalPartial", Model.OffersList[i].Offer)
}
@Scripts.Render("~/bundles/offer")
